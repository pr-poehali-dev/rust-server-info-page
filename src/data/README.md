# Руководство по редактированию данных сайта

Этот каталог содержит JSON файлы для управления контентом сайта без необходимости изменения кода.

## 📁 Файлы данных

### 1. `servers.json` - Данные серверов

Управление информацией о серверах Rust.

#### Структура:

```json
{
  "pveServers": [...],
  "pvpServers": [...],
  "defaultDetailedDescription": {...}
}
```

#### Как добавить новый сервер:

1. Откройте `servers.json`
2. Добавьте новый объект в массив `pveServers` или `pvpServers`:

```json
{
  "id": "10",
  "name": "#10 [PVE] DevilRust X50",
  "mode": "PVE x50",
  "ip": "62.122.214.220:10000",
  "battlemetricsId": "12345678",
  "description": "Описание сервера",
  "features": [
    "Рейты x50",
    "Вайп раз в неделю"
  ],
  "detailedDescription": {
    "title": "Полное описание сервера",
    "highlights": [
      { "icon": "Zap", "text": "Моментальный крафт" },
      { "icon": "Users", "text": "Кастомные NPC" }
    ],
    "description": ""
  }
}
```

#### Как изменить описание сервера:

1. Найдите сервер по `id`
2. Измените поля:
   - `name` - название сервера
   - `description` - краткое описание
   - `features` - список особенностей (массив строк)
   - `detailedDescription.highlights` - расширенные возможности

#### Доступные иконки для highlights:

- `Users`, `Users2` - игроки
- `Zap` - молния/энергия
- `Home` - дом
- `Layers` - слои
- `Lock` - замок
- `TreeDeciduous` - дерево
- `Shield`, `ShieldAlert` - щит
- `Sparkles` - искры
- `Store` - магазин
- `Trophy` - трофей
- `Map` - карта
- `Wrench` - инструменты
- `Target` - цель
- `Coins` - монеты
- `Calendar` - календарь
- `Gift` - подарок
- `Swords` - мечи
- `Heart` - сердце
- `Truck` - транспорт
- `Building` - строительство
- `Pickaxe` - кирка
- `Backpack` - рюкзак
- `Radio` - радио
- `Timer` - таймер
- `Settings` - настройки
- `Navigation` - навигация

---

### 2. `banlist.json` - Настройки банлиста

Управление отображением и текстами страницы банлиста.

#### Что можно изменить:

```json
{
  "apiUrl": "URL API для получения банов",
  "title": "Заголовок страницы",
  "subtitle": "Подзаголовок",
  "searchPlaceholder": "Текст в поиске",
  "tableHeaders": {
    "nickname": "Заголовок колонки никнейм",
    "steamId": "Заголовок колонки Steam ID",
    ...
  },
  "messages": {
    "loading": "Текст при загрузке",
    "noResults": "Текст когда ничего не найдено",
    ...
  }
}
```

#### Пример изменения текста:

Изменить placeholder поиска:
```json
"searchPlaceholder": "Введите ник или ID..."
```

---

### 3. `header.json` - Настройки шапки сайта

Управление логотипом, меню навигации и авторизацией.

#### Изменение логотипа:

```json
"logo": {
  "imageUrl": "https://example.com/logo.png",
  "alt": "DevilRust",
  "text": "DEVILRUST",
  "fontFamily": "Nunito, sans-serif",
  "fontStyle": "italic"
}
```

#### Добавление пункта меню:

```json
"navigation": [
  {
    "id": "custom",
    "label": "Новый пункт",
    "href": "/custom",
    "external": false
  }
]
```

Если ссылка внешняя (на другой сайт), установите `"external": true`.

#### Изменение текста кнопок:

```json
"auth": {
  "buttons": {
    "login": "Войти через Steam",
    "profile": "Мой профиль",
    "alreadyLoggedIn": "Уже есть аккаунт?"
  }
}
```

#### Изменение URL авторизации:

```json
"auth": {
  "loginUrl": "https://devilrust.ru/api/v1/player.login?login",
  "profileUrl": "https://devilrust.ru/profile",
  ...
}
```

---

## ⚠️ Важные правила

1. **JSON синтаксис**: 
   - Все строки в двойных кавычках `"text"`
   - Последний элемент в массиве БЕЗ запятой
   - Проверяйте валидность JSON после изменений

2. **ID серверов**:
   - Должны быть уникальными
   - Используются для связи с системой мониторинга

3. **Иконки**:
   - Используйте только иконки из списка выше
   - Регистр имеет значение: `Users`, НЕ `users`

4. **Резервные копии**:
   - Перед изменениями сделайте копию файла
   - Храните копии в безопасном месте

---

## 🔧 Проверка изменений

После изменения JSON файлов:

1. Проверьте валидность JSON на [jsonlint.com](https://jsonlint.com/)
2. Сохраните файл
3. Обновите страницу сайта
4. Проверьте, что всё отображается корректно

---

## 💡 Примеры частых изменений

### Изменить рейты сервера:

```json
"features": [
  "Рейты x100",  // было x50
  "Вайп раз в неделю"
]
```

### Добавить особенность сервера:

```json
"detailedDescription": {
  "highlights": [
    { "icon": "Zap", "text": "Моментальный крафт" },
    { "icon": "Gift", "text": "Ежедневные бонусы" }  // добавили
  ]
}
```

### Изменить название меню:

```json
"navigation": [
  {
    "id": "home",
    "label": "На главную",  // было "Главная"
    "href": "/"
  }
]
```

---

### 4. `promotion.json` - Всплывающее уведомление об акции

Управление промо-уведомлением с таймером обратного отсчета.

#### Структура:

```json
{
  "enabled": true,
  "title": "Временная x2 АКЦИЯ!",
  "subtitle": "При пополнении баланса...",
  "startDate": "2024-12-11T00:00:00",
  "endDate": "2024-12-12T23:59:59",
  "button": {
    "text": "Перейти в магазин",
    "url": "https://devrus.gamestores.app/"
  }
}
```

#### Включить/выключить акцию:

```json
"enabled": true  // false чтобы скрыть
```

#### Изменить текст акции:

```json
"title": "🔥 МЕГА РАСПРОДАЖА!",
"subtitle": "Скидка 50% на все товары до конца недели"
```

#### Установить период акции:

```json
"startDate": "2024-12-15T00:00:00",
"endDate": "2024-12-31T23:59:59"
```

**Важно**: Формат даты строго `YYYY-MM-DDTHH:MM:SS`

#### Изменить кнопку:

```json
"button": {
  "text": "Купить сейчас",
  "url": "https://ваша-ссылка.com"
}
```

#### Настройки отображения:

```json
"styling": {
  "showGifts": true,
  "accentColor": "#FF4400",
  "animation": "pulse"
}
```

#### Поведение уведомления:

```json
"behavior": {
  "showOnce": false,
  "cookieName": "devilrust_promotion_seen"
}
```

Если `showOnce: true`, пользователь увидит акцию только один раз.

---

## 📞 Поддержка

Если у вас возникли вопросы или проблемы с редактированием:
- Проверьте синтаксис JSON
- Убедитесь, что используете правильные иконки
- Сделайте резервную копию перед экспериментами